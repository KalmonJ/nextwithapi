import { Button } from "components/Button";
import { Header } from "components/Header";

export const Home = () => {
  return (
    <main>
      <Header></Header>
    </main>
  );
};

export default Home;
